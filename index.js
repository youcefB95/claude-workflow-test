// Create une fonction qui calcule le carré d'un nombre

/**
 * Calcule le carré d'un nombre
 * @param {number} nombre - Le nombre à élever au carré
 * @returns {number} Le carré du nombre
 */
function carre(nombre) {
  return nombre * nombre;
}

module.exports = { carre };
